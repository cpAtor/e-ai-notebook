# Zero network by default

The MVP application shell will make zero runtime network calls by default and will bundle runtime dependencies locally rather than loading app libraries or assets from CDNs. Network access is only introduced after explicit user action, such as configuring AI model access or importing external material, so the Private by Default and offline-friendly product promises are technically reflected in the architecture.
